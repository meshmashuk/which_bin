# One postcode per line. Lines starting with # are ignored.
#
# Collection days are the same for every address sharing a postcode (a
# fixed area-wide round), so one postcode covers everyone registered there.
#
# Copy this file to scripts/neighbours.txt (gitignored) and fill in real
# postcodes, then run:
#   npm run refresh -- --batch scripts/neighbours.txt

BN6 8JZ
BN6 8JY
